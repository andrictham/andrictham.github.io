import React from 'react'
import Markdown from 'react-remarkable'

function _getProjects(array) {
    let result = {}
    array.map(
      (project) => {

        return true
      }

    )
    return (
      <div className="projects">

      </div>
    )
  }

export default _getProjects
